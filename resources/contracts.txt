load(variables)
- variables != null and not Empty()
- variables.getId() notBlank() 
- getDomain(), getInitials() notEmpty()
- getTransitionBlock() != null


generateOriginalGraph:
- variablesMap, transitionBlockMap notEmpty()
- keys(variablesMap) == keys(transitionBlockMap)

generateInitialAbstraction
- variablesMap, transitionBlockMap notEmpty()
- keys(variablesMap) == keys(transitionBlockMap)

getTransitionBlockMap()
- transitionBlockMap notEmpty()

getvariablesMap()
- variablesMap notEmpty()

getVariable(id)
- variablesMap notEmpty()

getImage:
- TransitionBlockMap() != null

splitPATH:
- abstractionGraph != null
- graph.getInitialTuples() > 0
-> checks if Path is Valid && graph != null

refine
- failureState is a state with #h-1(s) > #dead ends
- deadEnds is not null

